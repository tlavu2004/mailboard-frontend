# API Configuration
# When using Nginx reverse proxy on port 80, this can be relative or point to localhost:80
NEXT_PUBLIC_API_URL=

# Google OAuth Configuration
# Matches the Client ID in the backend
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# App base URL (used for OAuth redirect URIs)
NEXT_PUBLIC_APP_URL=
